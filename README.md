# -Excel-Project-Dynamic-Range-Auto-Refresh-Dashboard
This repository presents an Excel project where all dashboards and visualizations update themselves automatically as soon as new data is entered. Using dynamic named ranges and auto-refresh methods, it enables zero manual intervention for data source changes — ensuring that charts, tables, and summaries always reflect the latest data.
